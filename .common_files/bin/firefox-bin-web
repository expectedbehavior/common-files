#!/bin/bash

script=`basename $0`
profile="${script/firefox-bin-/}"

# sleep so you're not still holding the option key down when we launch FF (causing safe mode)
sleep 1

exec /Applications/Firefox.app/Contents/MacOS/firefox-bin -P $profile
